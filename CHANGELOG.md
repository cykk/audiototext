# Changelog

## 0.1.0 - 2026-06-30

- Rebuilt the repository as a standalone `audiototext` Python CLI project.
- Replaced the legacy notebook-first layout with a package under `src/audiototext`.
- Added a standard `pyproject.toml`, MIT license, basic tests, and GitHub Actions CI.
- Rewrote the README for a self-hosted CLI workflow and linked naturally to `https://mp3totext.ai/`.
- Removed legacy example media, generated outputs, Jekyll config, and upstream funding metadata.
